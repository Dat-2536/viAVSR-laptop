# Vietnamese tokenizer assets

Place the verified Vietnamese tokenizer files here, for example:

```text
unigram2048.model
unigram2048_units.txt
```

The actual tokenizer binaries are ignored by Git.
